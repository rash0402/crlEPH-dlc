# --- OS & Tool Specific ---
.DS_Store
**/.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
venv/
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
requirements.txt
# (Note: requirements.txt is usually tracked, but if user wants minimal tree, keeping it is standard practice. Included here for review, will keep it tracked by NOT ignoring it in practice, removing line below)
# requirements.txt 

# --- Julia ---
*.jl.cov
*.jl.*.cov
*.jl.mem
Manifest.toml
**/Manifest.toml
deps/
docs/build/
docs/site/
LocalPreferences.toml

# --- Project Specific Data & Logs ---
# Ignore all generated data
data/logs/*.h5
data/logs/*.log
data/vae_training/*.h5

# Ignore large model checkpoints but keep the best one
models/checkpoints/
models/vae_latest.bson
models/*.bson
!models/action_vae_best.bson

# Ignore archives
scripts/archive/
results/archive/

# Ignore temporary files
tmp/
*.log

# Ignore large media
*.webp
*.mp4
*.avi
recordings/

# --- Artifacts ---
# Keep specific results if needed, but generally ignore heavy outputs
# results/haze_validation/*.png (Let's keep validation plots for record)
