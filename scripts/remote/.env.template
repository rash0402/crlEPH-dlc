# Remote Server Configuration
# Copy this file to .env and fill in the details

# Connection Details
REMOTE_HOST=192.168.1.XXX
REMOTE_USER=username
REMOTE_PORT=22

# Workspace Paths
# The directory on the remote server where the project will be synced
REMOTE_DIR=/home/username/work/crlEPH-dlc

# Execution Mode: 'direct' or 'docker'
EXECUTION_MODE=direct

# Docker Configuration (only used if EXECUTION_MODE=docker)
DOCKER_IMAGE_NAME=crleph-dlc
DOCKER_TAG=latest
# Additional docker run flags (e.g. --gpus all)
DOCKER_FLAGS="--gpus all"
