# EPH Scripts Documentation

このディレクトリには、EPH（Emergent Perceptual Haze）プロジェクトの実行・検証・実験のためのスクリプトが格納されています。

**Phase理論やアーキテクチャの詳細は [doc/PHASE_GUIDE.md](../doc/PHASE_GUIDE.md) を参照してください。**

---

## 📋 目次

1. [クイックスタート](#クイックスタート)
2. [コアスクリプト](#コアスクリプト)
3. [検証・テスト](#検証テスト)
4. [実験・解析](#実験解析)
5. [GRU予測器](#gru予測器)
6. [ワークフロー](#ワークフロー)
7. [トラブルシューティング](#トラブルシューティング)

---

## クイックスタート

### 環境構築

```bash
# 環境セットアップ（初回のみ）
./scripts/setup_env.sh
```

### 基本的な動作確認

```bash
# 全Phaseの検証テスト（コミット前に必ず実行）
./scripts/run_basic_validation.sh all

# GUIから実行する場合
./scripts/start_gui.sh
```

### シミュレーション実行

```bash
# Julia EPHサーバー + Python Viewerの同時起動
./scripts/run_experiment.sh
```

---

## コアスクリプト

### `run_server.sh`

**目的:** Julia EPHサーバーを単体で起動

```bash
./scripts/run_server.sh
```

**動作:**
- ZeroMQ PUBソケットをポート5555で起動
- EPHシミュレーションを実行
- Pythonビューワーと通信可能な状態にする

**使用場面:**
- ビューワーなしでシミュレーションのみを実行したい場合
- カスタムクライアントから接続したい場合

---

### `run_viewer.sh`

**目的:** Python Viewerを単体で起動

```bash
./scripts/run_viewer.sh
```

**前提条件:**
- Julia EPHサーバーが既に起動していること（run_server.sh または別プロセス）

**動作:**
- ZeroMQ SUBソケットでポート5555に接続
- Pygameウィンドウで可視化

**使用場面:**
- サーバーは既に起動済みで、ビューワーのみを再起動したい場合

---

### `run_experiment.sh`

**目的:** サーバー + ビューワーを一括起動

```bash
./scripts/run_experiment.sh
```

**動作:**
1. Julia EPHサーバーをバックグラウンド起動
2. Python Viewerを起動
3. Ctrl+C で両方同時に終了

**推奨:** 通常のシミュレーション実行はこれを使用

---

### `start_gui.sh`

**目的:** EPH Dashboard GUIを起動

```bash
./scripts/start_gui.sh
```

**機能:**
- システム状態確認（Dependencies, Julia Packages）
- Validation実行（Phase 1-4）
- Experiments実行（Shepherding等）
- 実験結果の可視化

**推奨:** 研究実験の実行・管理にはGUIを使用

---

## 検証・テスト

### `run_basic_validation.sh`

**目的:** Phase 1-4の基礎機能検証

```bash
# 全Phase検証（推奨）
./scripts/run_basic_validation.sh all

# 個別Phase検証
./scripts/run_basic_validation.sh 1    # Phase 1: Scalar Self-Haze
./scripts/run_basic_validation.sh 2    # Phase 2: Environmental Haze
./scripts/run_basic_validation.sh 3    # Phase 3: Advanced Integration
./scripts/run_basic_validation.sh 4    # Phase 4: Full 3D Tensor Haze
```

**検証内容:**

| Phase | テスト数 | 検証内容 |
|-------|---------|---------|
| **Phase 1** | 3 | Scalar Self-Haze の基礎機能 |
| **Phase 2** | 5 | SPM生成・Environmental Haze |
| **Phase 3** | 6 | GRU予測器・Shepherding統合 |
| **Phase 4** | 6 | 3Dテンソルヘイズ・チャネル制御 |
| **合計** | **20** | - |

**成功基準:** 全テストがPASS

**実行タイミング:**
- コード修正後、Git commitの前に毎回実行
- Pull Request作成前
- 環境構築直後の動作確認

**詳細:** [doc/VALIDATION_PHILOSOPHY.md](../doc/VALIDATION_PHILOSOPHY.md) 参照

---

### `test_phase2_haze.jl`

**目的:** Phase 2ヘイズ機能の詳細テスト

```bash
julia --project=src_julia scripts/test_phase2_haze.jl
```

**内容:**
- Environmental hazeの堆積・減衰テスト
- SPMへのヘイズ影響テスト

---

## 実験・解析

### `run_shepherding_experiment.sh`

**目的:** Shepherding実験の実行

```bash
./scripts/run_shepherding_experiment.sh
```

**動作:**
1. 実験パラメータの入力プロンプト表示
2. Shepherding実験実行
3. 結果を `src_julia/data/experiments/` に保存

**出力:**
- メトリクス（成功率、タスク時間等）
- 軌跡データ
- 統計サマリー

**GUI推奨:** GUIの Experiments タブから実行する方が便利

---

### `shepherding_experiment.jl`

**目的:** Shepherding実験のJulia実装

```bash
julia --project=src_julia scripts/shepherding_experiment.jl
```

**内容:**
- 犬エージェント（ShepherdingEPH）による羊の誘導
- BoidsAgentによる群れ行動
- メトリクス収集・保存

**通常は `run_shepherding_experiment.sh` 経由で実行**

---

### `baseline_comparison.jl`

**目的:** EPHと他手法（Boids, Social Force等）の比較実験

```bash
julia --project=src_julia scripts/baseline_comparison.jl
```

**比較対象:**
- Boids（基本群れアルゴリズム）
- Social Force Model
- Potential Field
- EPH (Phase 1/2)

**出力:** 比較結果のプロット・統計データ

---

### `eph_parameter_optimization.jl`

**目的:** EPHパラメータの最適化

```bash
julia --project=src_julia scripts/eph_parameter_optimization.jl
```

**最適化対象:**
- γ (precision modulation exponent)
- α (sigmoid sensitivity)
- decay rates
- thresholds

**手法:** グリッドサーチまたはベイズ最適化

---

### `analyze_experiment.jl`

**目的:** 実験結果の統計解析

```bash
julia --project=src_julia scripts/analyze_experiment.jl
```

**機能:**
- メトリクスの集計
- 統計検定（t-test, ANOVA等）
- プロット生成

---

## GRU予測器

Phase 3では、GRUネットワークを用いた将来ヘイズ予測機能を使用します。

### `collect_gru_training_data.sh`

**目的:** GRU学習用データの収集（単一実行）

```bash
# デフォルト（20,000ステップ × 15エージェント）
./scripts/collect_gru_training_data.sh

# カスタムパラメータ
./scripts/collect_gru_training_data.sh 25000 20
```

**動作:**
1. 1回のシミュレーションを実行
2. SPMとヘイズの時系列データを収集
3. `data/training/` に保存

**データ形式:** JLD2

**デフォルト設定:** 20,000ステップ × 15エージェント

**注意:** 十分なデータ量を得るには、`collect_multiple_datasets.sh`の使用を推奨

---

### `collect_multiple_datasets.sh`

**目的:** 多様なGRU学習用データの自動収集（推奨）

```bash
# デフォルト（10回実行）
./scripts/collect_multiple_datasets.sh

# カスタム実行回数
./scripts/collect_multiple_datasets.sh 15
```

**動作:**
1. 複数回のシミュレーションを自動実行（デフォルト10回）
2. 各回で異なるパラメータを使用：
   - ステップ数: 16,000〜25,000
   - エージェント数: 10〜30
   - 多様な密度・相互作用パターン
3. すべてのデータを `data/training/` に蓄積

**推奨理由:**
- データ多様性確保（過学習防止）
- 十分なデータ量（理想: 200,000+ transitions）
- より汎用的なGRUモデルの学習が可能

**実行時間:** 約2-5時間（10回実行の場合）

**期待データ量:** 約2,000,000 transitions（10回 × 20,000 steps × 15 agents）

---

### `gru/update_gru.sh`

**目的:** GRUモデルの学習・更新（統合スクリプト）

```bash
./scripts/gru/update_gru.sh
```

**推奨:** GRU学習はこのスクリプトを使用

**内部動作:**
1. データ収集
2. 学習
3. モデル保存

---

### `gru/train_gru.jl`

**目的:** GRU予測器の学習（基本版）

```bash
julia --project=src_julia scripts/gru/train_gru.jl
```

**学習対象:** SPM → 将来ヘイズの予測

---

### `gru/pretrain_gru.jl`

**目的:** GRU予測器の事前学習

```bash
julia --project=src_julia scripts/gru/pretrain_gru.jl
```

**用途:** 大量の合成データで事前学習

---

### `gru/train_predictor.jl`

**目的:** SPMPredictor（LinearPredictor or NeuralPredictor）の学習

```bash
julia --project=src_julia scripts/gru/train_predictor.jl
```

---

### `gru/update_gru_model.jl`

**目的:** GRUモデルの更新（ファインチューニング）

```bash
julia --project=src_julia scripts/gru/update_gru_model.jl
```

---

### `gru/update_gru_weighted.jl`

**目的:** 重み付き学習によるGRU更新

```bash
julia --project=src_julia scripts/gru/update_gru_weighted.jl
```

**特徴:** 重要なサンプルに高い重みを付与

---

## ワークフロー

### `run_complete_workflow.sh`

**目的:** データ収集→GRU学習→実験の完全自動化

```bash
./scripts/run_complete_workflow.sh full
```

**動作フロー:**
1. 環境確認
2. Validation実行
3. データ収集
4. GRU学習
5. 実験実行
6. 結果解析

**所要時間:** 数時間（設定による）

**用途:** 夜間バッチ実行・CI/CD統合

---

### `setup_env.sh`

**目的:** 開発環境のセットアップ

```bash
./scripts/setup_env.sh
```

**動作:**
- Juliaパッケージのインストール
- Pythonパッケージのインストール
- ディレクトリ構造の作成

**実行タイミング:** プロジェクトクローン直後、初回のみ

---

## トラブルシューティング

### Validation が失敗する

**確認事項:**
```bash
# Juliaパッケージの再インストール
cd src_julia
julia --project=. -e 'using Pkg; Pkg.instantiate()'

# Pythonパッケージの確認
pip install -r requirements.txt

# 個別Phase検証でエラー箇所を特定
./scripts/run_basic_validation.sh 1
./scripts/run_basic_validation.sh 2
./scripts/run_basic_validation.sh 3
./scripts/run_basic_validation.sh 4
```

**よくあるエラー:**
- `UndefVarError: Statistics not defined` → モジュールのimport忘れ
- `MethodError` → 型の不一致
- `LoadError` → ファイルパスの問題

---

### GRUモデルが見つからない

**症状:** Phase 3 Test 3.5/3.6 でWARNING

**原因:** GRUモデルがまだ学習されていない

**対処:**
```bash
# GRUモデルを学習
./scripts/gru/update_gru.sh
```

**注:** Phase 3 Test 3.1-3.4 がPASSすればOK（3.5-3.6は任意）

---

### ポート5555が使用中

**エラー:** `Address already in use`

**対処:**
```bash
# ポート使用状況確認
lsof -i :5555

# プロセスを終了
kill -9 <PID>
```

---

### GUI起動時にPySide6エラー

**対処:**
```bash
# PySide6再インストール
pip install --upgrade PySide6
```

---

## Phase実装ステータス

| Phase | 実装状況 | 検証 | 実験 | 説明 |
|-------|---------|------|------|------|
| **Phase 1** | ✅ 完了 | ✅ Pass | ✅ 実施済み | Scalar Self-Haze |
| **Phase 2** | ✅ 完了 | ✅ Pass | ✅ 実施済み | Environmental Haze + SPM |
| **Phase 3** | ✅ 完了 | ✅ Pass | 🔄 進行中 | GRU予測器 + Shepherding |
| **Phase 4** | ✅ 完了 | ✅ Pass | 📋 未実施 | Full 3D Tensor Haze |

**詳細:** [doc/PHASE_GUIDE.md](../doc/PHASE_GUIDE.md) 参照

---

## 参考ドキュメント

- **[doc/PHASE_GUIDE.md](../doc/PHASE_GUIDE.md)** - Phase理論・アーキテクチャの詳細
- **[doc/VALIDATION_PHILOSOPHY.md](../doc/VALIDATION_PHILOSOPHY.md)** - Validationの目的と意義
- **[CLAUDE.md](../CLAUDE.md)** - プロジェクト全体の概要
- **[README.md](../README.md)** - プロジェクトのトップレベル説明

---

## 開発ワークフロー

```
┌─────────────────────────────────────────┐
│  推奨開発サイクル                         │
├─────────────────────────────────────────┤
│  1. コード修正                           │
│     ↓                                   │
│  2. Validation 実行                     │
│     ./scripts/run_basic_validation.sh all│
│     ↓ (全テストPass)                    │
│  3. Git commit                          │
│     ↓                                   │
│  4. 必要に応じて Experiments 実行        │
│     ./scripts/start_gui.sh              │
│     → Experiments タブから実行          │
└─────────────────────────────────────────┘
```

**重要:** Validation が Pass しないコードは commit しないこと！

---

## 連絡先・フィードバック

- **Issues:** GitHubリポジトリのIssuesで報告
- **ドキュメント更新:** Pull Request歓迎

---

**Last Updated:** 2025-11-24
**Version:** Phase 1-4 完全実装版
